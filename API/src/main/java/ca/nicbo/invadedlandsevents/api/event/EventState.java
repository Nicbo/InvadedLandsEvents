package ca.nicbo.invadedlandsevents.api.event;

/**
 * The different states an {@link Event} can be in.
 *
 * @author Nicbo
 */
public enum EventState {
    WAITING,
    COUNTDOWN,
    STARTED,
    ENDED,
    STOPPED
}
