package me.nicbo.invadedlandsevents.util.misc;

/**
 * Interface that represents a builder class
 *
 * @author Nicbo
 * @param <T> the type that the builder builds
 */

public interface Builder<T> {
    T build();
}
